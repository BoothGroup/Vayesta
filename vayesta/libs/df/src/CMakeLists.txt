target_sources(df PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/df_jk.c)

set_target_properties(df PROPERTIES
  LIBRARY_OUTPUT_DIRECTORY ${PROJECT_SOURCE_DIR}
  COMPILE_FLAGS ${OpenMP_C_FLAGS}
  LINK_FLAGS ${OpenMP_C_FLAGS})
